{
  "name": "entity-typescript-cleanroom",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node dist/test.js",
    "verify": "node dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  }
}